spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tg_db
    password: tg_pwd
    username: tg_user
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true
#  data:
#    redis:
#      host: localhost
#      port: 6379

server:
  port: 8181